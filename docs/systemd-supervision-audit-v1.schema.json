{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://grimnir.gille.ai/contracts/systemd-supervision-audit/v1/schema.json",
  "title": "Brokkr systemd supervision audit v1",
  "description": "Read-only audit projection containing registry-derived classification, content-blind state evidence, freshness, and routed findings.",
  "type": "object",
  "additionalProperties": false,
  "required": ["kind", "schema_version", "baseline_id", "topology_authority", "observed_at", "evaluated_at", "freshness", "notifier", "summary", "units", "findings", "extensions"],
  "properties": {
    "kind": { "const": "systemd-supervision-audit" },
    "schema_version": { "const": "v1" },
    "baseline_id": { "$ref": "#/$defs/id" },
    "topology_authority": { "const": "grimnir-service-registry" },
    "observed_at": { "$ref": "#/$defs/utc" },
    "evaluated_at": { "$ref": "#/$defs/utc" },
    "freshness": { "$ref": "#/$defs/freshness" },
    "notifier": { "$ref": "#/$defs/notifier" },
    "summary": { "$ref": "#/$defs/summary" },
    "units": { "type": "array", "items": { "$ref": "#/$defs/unit" } },
    "findings": { "type": "array", "items": { "$ref": "#/$defs/finding" } },
    "extensions": { "type": "array", "items": {} }
  },
  "$defs": {
    "id": { "type": "string", "pattern": "^[a-z][a-z0-9-]{2,62}$" },
    "utc": { "type": "string", "pattern": "^\\d{4}-\\d\\d-\\d\\dT\\d\\d:\\d\\d:\\d\\dZ$", "format": "date-time" },
    "freshness": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "age_seconds", "max_age_seconds"],
      "properties": {
        "status": { "enum": ["fresh", "stale", "future"] },
        "age_seconds": { "type": "integer", "minimum": 0 },
        "max_age_seconds": { "type": "integer", "minimum": 1 }
      }
    },
    "notifier": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status"],
      "properties": { "status": { "enum": ["available", "absent", "unknown"] } }
    },
    "summary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "unit_count", "compliant_unit_count", "finding_count"],
      "properties": {
        "status": { "enum": ["pass", "fail"] },
        "unit_count": { "type": "integer", "minimum": 0 },
        "compliant_unit_count": { "type": "integer", "minimum": 0 },
        "finding_count": { "type": "integer", "minimum": 0 }
      }
    },
    "finding-core": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "severity", "route"],
      "properties": {
        "code": { "type": "string", "pattern": "^[a-z][a-z0-9_]{2,63}$" },
        "severity": { "enum": ["error", "warning"] },
        "route": { "enum": ["substrate", "component-owner"] }
      }
    },
    "finding": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "severity", "route"],
      "properties": {
        "code": { "type": "string", "pattern": "^[a-z][a-z0-9_]{2,63}$" },
        "severity": { "enum": ["error", "warning"] },
        "route": { "enum": ["substrate", "component-owner"] },
        "unit": { "type": "string", "pattern": "^[A-Za-z0-9:_.@-]+\\.(?:service|timer)$" },
        "owner": { "$ref": "#/$defs/id" }
      }
    },
    "unit-finding": { "$ref": "#/$defs/finding-core" },
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "required": ["unit_result", "restart", "watchdog", "oom", "timer"],
      "properties": {
        "unit_result": { "$ref": "#/$defs/unit-result" },
        "restart": { "$ref": "#/$defs/restart" },
        "watchdog": { "$ref": "#/$defs/watchdog" },
        "oom": { "$ref": "#/$defs/oom" },
        "timer": { "oneOf": [{ "type": "null" }, { "$ref": "#/$defs/timer" }] }
      }
    },
    "unit-result": {
      "type": "object",
      "additionalProperties": false,
      "required": ["active_state", "sub_state", "result"],
      "properties": {
        "active_state": { "type": "string" },
        "sub_state": { "type": "string" },
        "result": { "type": "string" }
      }
    },
    "restart": {
      "type": "object",
      "additionalProperties": false,
      "required": ["count", "window_start", "window_end"],
      "properties": {
        "count": { "type": "integer", "minimum": 0 },
        "window_start": { "$ref": "#/$defs/utc" },
        "window_end": { "$ref": "#/$defs/utc" }
      }
    },
    "watchdog": { "type": "object", "additionalProperties": false, "required": ["result"], "properties": { "result": { "type": "string" } } },
    "oom": { "type": "object", "additionalProperties": false, "required": ["result"], "properties": { "result": { "type": "string" } } },
    "timer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["last_run_at", "next_run_at", "last_result", "missed_runs", "persistent"],
      "properties": {
        "last_run_at": { "oneOf": [{ "type": "null" }, { "$ref": "#/$defs/utc" }] },
        "next_run_at": { "oneOf": [{ "type": "null" }, { "$ref": "#/$defs/utc" }] },
        "last_result": { "type": "string" },
        "missed_runs": { "type": "integer", "minimum": 0 },
        "persistent": { "type": "boolean" }
      }
    },
    "unit": {
      "type": "object",
      "additionalProperties": false,
      "required": ["unit", "owner", "scope", "workload_shape", "status", "findings", "evidence"],
      "properties": {
        "unit": { "type": "string", "pattern": "^[A-Za-z0-9:_.@-]+\\.(?:service|timer)$" },
        "owner": { "$ref": "#/$defs/id" },
        "scope": { "enum": ["system", "user"] },
        "workload_shape": { "enum": ["long-running", "oneshot", "timer"] },
        "status": { "enum": ["pass", "fail"] },
        "findings": { "type": "array", "items": { "$ref": "#/$defs/unit-finding" } },
        "evidence": { "oneOf": [{ "type": "null" }, { "$ref": "#/$defs/evidence" }] }
      }
    }
  }
}
